apiVersion: v1
kind: Service
metadata:
  name: openbank-frontend
  namespace: openbank
spec:
  selector:
    app: openbank-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP